# Public client env (safe to commit empty placeholders).
# Copy to `.env` for local overrides; set real values in EAS Environment Variables.

# Optional Sentry DSN. Leave empty to keep crash reporting off.
# Enabling reporting also needs a native build that includes @sentry/react-native.
EXPO_PUBLIC_SENTRY_DSN=
